import { ResolvedHand, WinState } from "./types";

// Computes all possible hands that could be made from the given state.
// Does not take into account whether or not the hands have yaku.
export function resolveHands(state: WinState): ResolvedHand[] {
    return [];
}